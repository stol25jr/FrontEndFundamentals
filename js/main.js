const buttonElement = document.getElementById("button");

buttonElement.addEventListener("click", clickFunction);
{
  localStorage.product = button;
}

function clickFunction() {
  console.log("The button was clicked!");
}
